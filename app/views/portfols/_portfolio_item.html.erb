<div class="col-md-4 car" data-id="<%= portfolio_item.id %>">
	<div class="card mb-4 shadow-sm">
		<%= image_tag portfolio_item.thumb_image unless portfolio_item.thumb_image.nil? %>
		<div class="card-body">
		  <p class="card-text"><%= link_to portfolio_item.title, portfol_show_path(portfolio_item) %></p>
		  <!-- <div class="d-flex justify-content-between align-items-center">
		    <div class="btn-group">
		      <button type="button" class="btn btn-sm btn-outline-secondary"><%#= link_to "Edit", edit_portfol_path(portfolio_item) if logged_in?(:site_admin) %>
		      </button>
		      <button type="button" class="btn btn-sm btn-outline-secondary"><%#= link_to "Delete", portfol_path(portfolio_item), method: :delete, data: { confirm: 'Are you sure?' } if logged_in?(:site_admin) %>
		      </button>
		    </div>
		  </div> -->
		</div>
	</div>
</div>





	      
