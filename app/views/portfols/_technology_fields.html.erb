<div class="form-group nested-fields">
	<%= f.label :name %>
	<%= f.text_field :name, class: "form-control" %>
	<%= link_to_remove_association 'Remove', f %>
</div>

<!-- when using cocoon gem and need to delete added fields... nested-fields class must be added to the div class -->